# Java-IDE-API-AbstractionLayer
Requirements:

Maven has to be on environment-path: See https://maven.apache.org/install.html for further details. If maven is not on environment-path 'mvn' cmd won't be found.

